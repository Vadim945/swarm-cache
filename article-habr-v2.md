# Статья для Habr v2.0 — редактура (stop-slop + copy-editing)

> Отредактированный вариант. Готов к вставке в редактор Хабра (HTML ниже в конце файла).

## Заголовок
**Я собрал ИИ-сервис, который запоминает ответы. Повторные вопросы — за 15 мс и в 10 раз дешевле**

## Текст

Любой сервис с ИИ отвечает на одни и те же вопросы по кругу. Клиенты спрашивают, поддержка отвечает, а счёт за API растёт — в том числе за ответы, которые уже давались.

Я собрал сервис, который это лечит: **Swarm Cache** — ИИ, который запоминает ответы.

**Как это работает**

Новый вопрос уходит в нейросеть. Ответ сохраняется в семантическую базу. Когда вопрос приходит снова — или похожий по смыслу — сервис достаёт готовый ответ из памяти и не вызывает ИИ.

Система сравнивает смысл, а не строки: «объясни, как работает attention» и «что такое attention в трансформерах» — один и тот же вопрос. Эмбеддинги считаются локально, порог сходства настраивается под задачу.

![Как работает семантический кэш](https://habrastorage.org/getpro/habr/upload_files/953/0ff/419/9530ff41934f80a3c93feb7abda8d9a4.png)

**Что получается на практике:**

- Новый вопрос (генерация LLM): 5–9 секунд, полная стоимость
- Повторный вопрос (кэш-хит): 12–30 миллисекунд, 10% от стоимости
- Экономия токенов на серии из 10+ повторных запросов: 70–80%

Итого: повторный ответ приходит в **300 раз быстрее** и стоит **в 10 раз дешевле**.

**Дальше — интереснее: рой**

Я пошёл дальше и сделал P2P-обмен. Несколько агентов (каждый со своим кэшем) объединены в рой через IPFS pubsub. Агент A сгенерировал ответ — опубликовал его в сеть. Агент B отвечает на тот же вопрос мгновенно, не тратя ни токена, ни секунды.

Между агентами — микро-платежи в духе L402: агент платит автору ответа 1 сатоши. Это задел на будущее: открытый рой, где каждый участник одновременно и потребитель, и поставщик.

![Рой: один спросил — знают все](https://habrastorage.org/getpro/habr/upload_files/d2b/48d/787/d2b48d78707ba00f5b7f915f7c7ea054.png)

**Что внутри (стек)**

- Семантический кэш: SQLite + sqlite-vec, локальные эмбеддинги (multilingual MiniLM)
- LLM: DeepSeek V4 Flash через OpenAI-совместимый API
- P2P: IPFS kubo + pubsub, DAG-публикация ответов
- API-ключи, биллинг, rate limiting, self-serve регистрация

**Можно попробовать прямо сейчас**

https://swarm.telscan.ru — на сайте есть демо. Без регистрации — 3 бесплатных вопроса в день. Ввёл имя — получил ключ и 10 вопросов сразу.

Подключение — один HTTP-запрос:

```
curl -X POST https://swarm.telscan.ru/ask \
  -H "X-API-Key: ***" \
  -H "Content-Type: application/json" \
  -d '{"question":"Что такое Attention?"}'
```

Код открыт: https://github.com/Vadim945/swarm-cache

**Кому это нужно**

- Чат-ботам и поддержке: клиенты задают одно и то же — отвечайте из кэша
- FAQ и справочникам: сотни одинаковых вопросов, мгновенные ответы
- Любому сервису с ИИ: меньше расходов на API, быстрее отклик

Главная идея: вопросы накапливаются как капитал. Чем их больше — тем быстрее и дешевле работает сервис.

---

## Что изменено и почему

| Было | Стало | Причина |
|---|---|---|
| «Почти любой сервис… отвечает снова и снова. Клиенты спрашивают одно и то же, поддержка отвечает одинаково» | «Любой сервис… отвечает по кругу. Клиенты спрашивают, поддержка отвечает, а счёт за API растёт» | Убран тройной повтор мысли; добавлена конкретика (счёт за API) |
| «Это не тупое сравнение строк» | «Система сравнивает смысл, а не строки» | Убран бинарный контраст «не X, а Y» |
| «Эмбеддинги считаются локально, порог сходства калибруется» | «Эмбеддинги считаются локально, порог сходства настраивается под задачу» | Пассив → актив, добавлен актор |
| «автору начисляется 1 сатоши» | «агент платит автору 1 сатоши» | Пассив → актив |
| «Цифры, которые получаются на практике» | «Что получается на практике» | Убрана вода |
| «0.1 от стоимости» | «10% от стоимости» | Консистентность |
| «Экономия токенов на серии повторных запросов: 70–80%» | «на серии из 10+ повторных запросов» | Конкретика (Sweep 5) |
| «3 бесплатных вопроса в день без регистрации» + «получил ключ и 10 бесплатных вопросов» | «Без регистрации — 3 вопроса в день. Ввёл имя — ключ и 10 вопросов сразу» | Устранено противоречие (Sweep 4) |
| «Вопросы не сгорают, они накапливаются как капитал» | «Вопросы накапливаются как капитал. Чем их больше — тем быстрее и дешевле» | Убрано драматическое отрицание, смысл сохранён |

---

## HTML для вставки в редактор Хабра

```html
<p>Любой сервис с ИИ отвечает на одни и те же вопросы по кругу. Клиенты спрашивают, поддержка отвечает, а счёт за API растёт — в том числе за ответы, которые уже давались.</p>
<p>Я собрал сервис, который это лечит: <strong>Swarm Cache</strong> — ИИ, который запоминает ответы.</p>
<h2>Как это работает</h2>
<p>Новый вопрос уходит в нейросеть. Ответ сохраняется в семантическую базу. Когда вопрос приходит снова — или похожий по смыслу — сервис достаёт готовый ответ из памяти и не вызывает ИИ.</p>
<p>Система сравнивает смысл, а не строки: «объясни, как работает attention» и «что такое attention в трансформерах» — один и тот же вопрос. Эмбеддинги считаются локально, порог сходства настраивается под задачу.</p>
<figure class="image"><img src="https://habrastorage.org/getpro/habr/upload_files/953/0ff/419/9530ff41934f80a3c93feb7abda8d9a4.png" alt="Как работает семантический кэш"><figcaption>Как работает семантический кэш</figcaption></figure>
<h2>Что получается на практике</h2>
<ul>
<li>Новый вопрос (генерация LLM): 5–9 секунд, полная стоимость</li>
<li>Повторный вопрос (кэш-хит): 12–30 миллисекунд, 10% от стоимости</li>
<li>Экономия токенов на серии из 10+ повторных запросов: 70–80%</li>
</ul>
<p>Итого: повторный ответ приходит в <strong>300 раз быстрее</strong> и стоит <strong>в 10 раз дешевле</strong>.</p>
<h2>Дальше — интереснее: рой</h2>
<p>Я пошёл дальше и сделал P2P-обмен. Несколько агентов (каждый со своим кэшем) объединены в рой через IPFS pubsub. Агент A сгенерировал ответ — опубликовал его в сеть. Агент B отвечает на тот же вопрос мгновенно, не тратя ни токена, ни секунды.</p>
<p>Между агентами — микро-платежи в духе L402: агент платит автору ответа 1 сатоши. Это задел на будущее: открытый рой, где каждый участник одновременно и потребитель, и поставщик.</p>
<figure class="image"><img src="https://habrastorage.org/getpro/habr/upload_files/d2b/48d/787/d2b48d78707ba00f5b7f915f7c7ea054.png" alt="Рой: один спросил — знают все"><figcaption>Рой: один спросил — знают все</figcaption></figure>
<h2>Что внутри (стек)</h2>
<ul>
<li>Семантический кэш: SQLite + sqlite-vec, локальные эмбеддинги (multilingual MiniLM)</li>
<li>LLM: DeepSeek V4 Flash через OpenAI-совместимый API</li>
<li>P2P: IPFS kubo + pubsub, DAG-публикация ответов</li>
<li>API-ключи, биллинг, rate limiting, self-serve регистрация</li>
</ul>
<h2>Можно попробовать прямо сейчас</h2>
<p><a href="https://swarm.telscan.ru">https://swarm.telscan.ru</a> — на сайте есть демо. Без регистрации — 3 бесплатных вопроса в день. Ввёл имя — получил ключ и 10 вопросов сразу.</p>
<p>Подключение — один HTTP-запрос:</p>
<pre><code>curl -X POST https://swarm.telscan.ru/ask \
  -H "X-API-Key: ***" \
  -H "Content-Type: application/json" \
  -d '{"question":"Что такое Attention?"}'</code></pre>
<p>Код открыт: <a href="https://github.com/Vadim945/swarm-cache">https://github.com/Vadim945/swarm-cache</a></p>
<h2>Кому это нужно</h2>
<ul>
<li>Чат-ботам и поддержке: клиенты задают одно и то же — отвечайте из кэша</li>
<li>FAQ и справочникам: сотни одинаковых вопросов, мгновенные ответы</li>
<li>Любому сервису с ИИ: меньше расходов на API, быстрее отклик</li>
</ul>
<p>Главная идея: вопросы накапливаются как капитал. Чем их больше — тем быстрее и дешевле работает сервис.</p>
```
